<%= form_for @artist do |f| %>
    <%= f.label "Name" %>
    <%= f.text_field :name %>

    <%= f.label "Password" %>
    <%= f.password_field :password %>

    <%= f.label "Confirm Your Password" %>
    <%= f.password_field :

    <%= f.submit "Sign Up" %>

<% end %>